library(shiny)

# Source app.R from the project root so that `ui` and `server` are available
# to all test files in this directory.
# Tests must be run from the project root (see README for instructions).
source(file.path(getwd(), "app.R"))
